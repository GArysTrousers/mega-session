{
  "name": "mega-session",
  "version": "0.0.4",
  "description": "Redis Session Manager",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "rimraf ./dist && npx tsc",
    "test": "npx tsc && node --test",
    "test-q": "node --test",
    "test-c": "rimraf ./dist && npx tsc && node --test"
  },
  "author": "GArysTrousers",
  "license": "MIT",
  "devDependencies": {
    "@types/cookie": "^0.6.0",
    "@types/jsonwebtoken": "^9.0.5",
    "@types/node": "^20.6.3",
    "@types/uuid": "^9.0.7",
    "rimraf": "^5.0.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "cookie": "^0.6.0",
    "jsonwebtoken": "^9.0.2",
    "redis": "^4.6.11",
    "uuid": "^9.0.1"
  }
}
